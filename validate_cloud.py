"""
Este archivo ya no realiza validaciones contra APIs en la nube.
Se reemplazó por una nota porque el proyecto está configurado para
usar exclusivamente Ollama local. Mantener scripts que prueben claves
de nube o que contengan claves en claro no es seguro.

Si necesitas volver a introducir validaciones, crea un script nuevo
fuera del repositorio o en un área segura y evita almacenar claves en
texto plano.
"""