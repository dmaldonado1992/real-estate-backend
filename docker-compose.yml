version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  db:
    image: postgres:13
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=productsdb
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/persistencia/schema.sql:/docker-entrypoint-initdb.d/1-schema.sql
      - ./backend/persistencia/seed_data.sql:/docker-entrypoint-initdb.d/2-seed_data.sql

volumes:
  postgres_data:
# This backend-level docker-compose file has been deprecated.
# Use the top-level `docker-compose.yml` at the repository root which
# defines the full stack (mysql, backend, frontend) and does not include
# any cloud provider environment variables.

# The original file referenced OpenAI and Postgres. Those references were
# removed to ensure the project uses Ollama local only and the canonical
# compose file is the one at the repo root.